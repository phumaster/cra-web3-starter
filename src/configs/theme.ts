const dark = {
  colors: {
    background: 'black',
    textColor: 'rgba(0, 255, 0)',
  },
};

const light = {
  colors: {
    background: 'white',
    textColor: 'black',
  },
};

const theme = {
  dark,
  light,
};

export default theme;
